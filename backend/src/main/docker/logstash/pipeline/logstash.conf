input {
  tcp {
    port => 5000
    type => syslog
    codec => json_lines
  }
  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["${ELASTICSEARCH_HOST}:${ELASTICSEARCH_PORT}"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}
